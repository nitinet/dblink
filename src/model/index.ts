/**
 * Model Module
 *
 * This module provides classes for database connection management and transaction support.
 * It handles the lower-level database interaction components that support the ORM functionality.
 *
 * @module model
 */
import Connection from './Connection.js';

export { Connection };
